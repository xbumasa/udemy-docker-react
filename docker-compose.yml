version: '3'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - /var/node/app/node_modules
      - ./:/var/node/app
  tests:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - /var/node/app/node_modules
      - ./:/var/node/app
    command: ["npm", "run", "test"]
